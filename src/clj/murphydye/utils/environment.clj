(ns murphydye.utils.environment)

;; (defn default-env-setting [key]
;;   (let [default (-> env :defaults key)]
;;     (-> env key default)))


;; (default-env-setting :redis)
;; (default-env-setting :db)
;; ((default-env-setting :db) :local)
